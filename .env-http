# Edge EC2 Configuration for WhisperLive Proxy
# Fill in your actual values before running docker compose

# Edge EC2 public IP (using self-signed cert for testing)
DOMAIN=3.138.85.115

# Email for Let's Encrypt certificate notifications (not used with IP)
EMAIL=david.bryan.mar@gmail.com

# GPU instance private/public IP (separate g4dn.xlarge instance)
GPU_HOST=3.138.85.115

# WhisperLive port on GPU
GPU_PORT=9090

# Default Whisper model to use
MODEL=Systran/faster-whisper-small.en

# Language code (en, es, fr, etc.)
LANGUAGE=en

# Optional: Basic Auth (uncomment to enable)
# BASIC_AUTH_USER=admin
# BASIC_AUTH_PASS=changeme
