# WhisperLive Edge Proxy Configuration Template
# Copy this file to .env-http and fill in your values
# cp .env-http.example .env-http

# Domain (using IP for now, can use domain name later)
# This should be your GPU instance's private IP address
DOMAIN=<GPU_PRIVATE_IP>

# Email for Let's Encrypt (not used with self-signed certs)
EMAIL=<YOUR_EMAIL>

# GPU WhisperLive endpoint
# Private IP address of GPU instance running WhisperLive
GPU_HOST=<GPU_PRIVATE_IP>

# WhisperLive WebSocket port (default: 9090)
GPU_PORT=9090

# WhisperLive model settings (these are just defaults shown in UI)
# Note: Only small.en is supported on T4 GPU to avoid OOM crashes
MODEL=Systran/faster-whisper-small.en

# Language code (en for English)
LANGUAGE=en
